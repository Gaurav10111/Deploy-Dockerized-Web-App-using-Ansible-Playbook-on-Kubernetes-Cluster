apiVersion: v1
kind: Service
metadata:
  name: {{ service_name }}
  namespace: {{ namespace }}
spec:
  type: NodePort
  selector:
    app: {{ image_name }}
  ports:
  - port: 80
    targetPort: {{ container_port }}
    nodePort: 30007

